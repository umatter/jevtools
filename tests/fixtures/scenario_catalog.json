[
  {"type": "function", "function": {"name": "get_weather", "description": "Get the current weather for a city.",
    "parameters": {"type": "object", "required": ["city"], "properties": {
      "city": {"type": "string", "description": "The city name", "x-jev": {"default_from": "user.home_city"}},
      "unit": {"type": "string", "enum": ["celsius", "fahrenheit"], "default": "celsius", "description": "The temperature unit"}}}}},
  {"type": "function", "function": {"name": "send_email", "description": "Send an email from the user to one recipient.",
    "parameters": {"type": "object", "required": ["to", "subject", "body"], "properties": {
      "to": {"type": "string", "format": "email", "description": "The recipient's email address"},
      "subject": {"type": "string", "description": "The subject line"},
      "body": {"type": "string", "description": "The body text of the email"}}}}},
  {"type": "function", "function": {"name": "create_event", "description": "Create a calendar event and invite attendees.",
    "parameters": {"type": "object", "required": ["title", "start"], "properties": {
      "title": {"type": "string", "description": "The event title"},
      "start": {"type": "string", "format": "date-time", "description": "The start time of the event"},
      "duration_minutes": {"type": "integer", "minimum": 5, "maximum": 480, "default": 30, "description": "The length of the event in minutes"},
      "attendees": {"type": "array", "items": {"type": "string", "format": "email"}, "default": [], "description": "The invitees"}}}}},
  {"type": "function", "function": {"name": "transfer_funds", "description": "Move money between two of the user's own bank accounts.",
    "parameters": {"type": "object", "required": ["from_account", "to_account", "amount", "currency"], "properties": {
      "from_account": {"type": "string", "description": "The account the money is taken from"},
      "to_account": {"type": "string", "description": "The account the money goes to"},
      "amount": {"type": "string", "pattern": "^\\d+(\\.\\d{1,2})?$", "description": "The amount to move"},
      "currency": {"type": "string", "pattern": "^[A-Z]{3}$", "description": "The ISO 4217 currency code",
                   "x-jev": {"default_from": "from_account.currency"}}}},
    "x-jev": {"risk": "critical", "constraints": ["from_account != to_account", "amount <= from_account.balance"],
              "render": "{amount} {currency}: {from_account.nickname} → {to_account.nickname}",
              "confirm_template": "Transfer {amount} {currency} from {from_account.label} to {to_account.label}?"}}},
  {"type": "function", "function": {"name": "read_file", "description": "Open a file in the user's workspace.",
    "parameters": {"type": "object", "required": ["path"], "properties": {
      "path": {"type": "string", "description": "The workspace-relative file path"}}}}},
  {"type": "function", "function": {"name": "search_web", "description": "Search the public web.",
    "parameters": {"type": "object", "required": ["query"], "properties": {
      "query": {"type": "string", "description": "The search query"}}}}}
]
